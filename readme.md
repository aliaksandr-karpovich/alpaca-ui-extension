This is a small chrome extension with some small UI improvements to https://alpaca.markets website.

To install the extension, open chrome://extensions/ → load unpacked

Current set of features:
- Fixed table width on /account/positions page for large screens